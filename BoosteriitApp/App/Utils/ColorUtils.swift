
import SwiftUI

enum AppColors {
    static let gradientStart = Color(red: 0.7575, green: 0.56, blue: 0.98)
    static let gradientEnd = Color(red: 0.58, green: 0.76, blue: 0.98)
    static let icon = Color.black
    static let badge = Color.red
    static let filterText = Color(.systemBlue)
    static let chipBackground = Color.white.opacity(0.2)
}
